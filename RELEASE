0.0.1
  First tag

0.0.2
  Change the request parser I/F. From the returns to the members.

0.0.3
  Change the PHP settings
   - php          (configure --with pear)
   - php.ini      (include_path)
   - cockatoo.ini (include_path)

  Major change in Beak
   - Obsolete BeakMysql
   - Obsolute Selectable unique-key
   - Select by index
   - Selectable column

1.0.0
  PHP 5.4 support

  Separate redirect feature to "moved permanently (301)" and "moved temporary (301)"
   You can specify on CMS and Action
   [CMS]
    - "top page (301)" in each TEMPLATE
    - "error page (302)" in each TEMPLATE
    - "redirect (301)" in each PATH
    - "error page (302)" in eath PATH
   [Action]
    - setMovedPermanently($location)
    - setMovedTemporary($location)

1.1.0
   - Add layout template 
   - Add html5 widget
   - Redesign request parser
   - Avolish div.co-Wbody element in production.
